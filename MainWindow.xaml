<Window x:Class="pr7.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:pr7"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="0.1*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.4*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="0.2*"/>
        </Grid.ColumnDefinitions>
        <ListBox x:Name="users" Background="Gray" Grid.Column="0" Grid.Row="0"/>
        <ListBox x:Name="pole_message" Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="0" Background="Gray" />
        <Button x:Name="btn_send" Content="Отправить" Grid.Row="1" Grid.Column="2" Click="btn_send_Click" Background="DarkGreen"/>
        <TextBox x:Name="your_message" Grid.Row="1" Grid.Column="1" Background="DarkGreen"/>
    </Grid>
</Window>
